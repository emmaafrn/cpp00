#include <iostream>
#include <string>

int		main()
{
	std::string buff;
	std::cout << "ADD, SEARCH or EXIT ? :";
	std::cin >> buff;
	if (!buff.compare("ADD"))
	{

	}
	if (!buff.compare("SEARCH"))
	{

	}
	if (!buff.compare("EXIT"))
	{

	}

}